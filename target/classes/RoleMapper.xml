<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sql.dao.RoleDao">
    <select id="selectRoleByRid" resultType="com.sql.bean.Role">
        select rid, rname, area, star, age, sex, elemental_force
        from role
        where rid = #{rid}
    </select>

    <select id="selectAllRole" resultType="com.sql.bean.Role">
        select rid, rname, area, star, age, sex, elemental_force
        from role
    </select>
</mapper>